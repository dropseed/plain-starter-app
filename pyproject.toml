[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.13.0"
dependencies = [
  "plain",
  "plain.postgres",
  "plain.auth",
  "plain.code",
  "plain.passwords",
  "plain.sessions",
  "plain.admin",
  "plain.tailwind",
  "plain.htmx",
  "plain.elements",
  "plain.toolbar",
  "psycopg[binary]>=3.2.3",
  "plain-assets>=0.3.0",
  "plain-templates>=0.3.0",
]

[dependency-groups]
dev = ["plain-dev", "plain-pytest"]

[tool.plain.tailwind]
version = "4.2.1"

[tool.plain.code.oxc]
version = "1.77.0"
